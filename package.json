{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "concurrently \"pnpm --filter @workspace/api-server run dev\" \"pnpm --filter @workspace/learner-hub run dev\"",
    "build":  "vite build"
    
    
  },
  "private": true,
  "devDependencies": {
    "concurrently": "^9.2.3",
    "cross-env": "^10.1.0",
    "prettier": "^3.8.4",
    "typescript": "~5.9.3"
  },
  "dependencies": {
    "cloudinary": "^2.10.0"
  }
  
}
